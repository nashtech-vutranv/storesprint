import {LayoutColor, SideBarTheme, SideBarWidth} from '../constants/layout'

interface IUserSetting {
  rowTable: number
  layoutColor: LayoutColor
  leftSideBarTheme: SideBarTheme
  leftSideBarType: SideBarWidth
}

export type {IUserSetting}